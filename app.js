const a = require('./index.js');

//we can also import like this but when import using this method then our file's extension should be mjs means module js
// import a from "./index.mjs"

a.averavge(2,10);
a.percentage(605,60);
